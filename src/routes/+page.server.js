// import { error } from '@sveltejs/kit';

// // export const prerender = true;

// /** @type {import('./$types').PageServerLoad} */
// export async function load({ params }) {
//     const loadData = 'I am page server content';

//     if (loadData) {
//         return {
//             loadData
//         };
//     }

//     throw error(404, 'Not Found');
// }
